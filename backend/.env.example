# SoukSync Backend Environment
APP_NAME=SoukSync
DEBUG=true
API_PORT=8020
API_HOST=0.0.0.0
API_PREFIX=/api/v1

# Use port 5433 if running SouqSync DB via docker-compose (5432 may be used by other projects)
DATABASE_URL=postgresql+asyncpg://souksync:souksync@localhost:5433/souksync
DATABASE_ECHO=false

REDIS_URL=redis://localhost:6379/0

# Telegram Bot (from BotFather; required for webhook)
TELEGRAM_BOT_TOKEN=

JWT_SECRET=change-me-in-production
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

LOG_LEVEL=DEBUG
