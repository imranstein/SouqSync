## 2024-05-22 - Context-Aware Action Buttons
**Learning:** In data tables with repetitive actions (Edit/Delete), generic labels like "Edit" are insufficient for screen reader users. Adding the item name to the label (e.g., "Edit Product A") provides critical context.
**Action:** Always include the item name in `aria-label` for row-specific actions in lists and tables.
